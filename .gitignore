# Rust build output
/target/

# MSVC link artifacts next to the binary
*.pdb

# bluetooth-force runtime files (created next to the exe, machine-local)
btf.log
btf.log.1
btf_*.txt

# Editor / OS noise
.vscode/
*.orig
Thumbs.db

# Local-only legacy Zig copy (never commit, see branch zig)
zig/
